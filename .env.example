# Pi-hole v6 environment variables
# Copy to .env and set your values:  cp .env.example .env
#
# Note: In docker-compose.yml, PIHOLE_PASSWORD is mapped to
#       FTLCONF_webserver_api_password (Pi-hole v6 syntax)

# Web interface password (leave empty string for no password)
# Mapped to: FTLCONF_webserver_api_password
PIHOLE_PASSWORD=changeme

# Timezone — https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York
